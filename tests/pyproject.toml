[project]
name = "debuai"
version = "0.1.1"
dependencies = [
    "typer",
    "rich",
    "openai",
    "anthropic"
]

[project.optional-dependencies]
dev = [
    "pytest",
    "pytest-cov"
]

[project.scripts]
debuai = "debugai.cli:app"
